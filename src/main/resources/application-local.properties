spring.datasource.url=jdbc:mysql://localhost:3306/eva_roadmap_test?createDatabaseIfNotExist=true
spring.datasource.username=root
# spring.datasource.password=Digicell@123
spring.datasource.password=r00t
# HikariCP Connection Pool - Single Connection Configuration
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000


# server.port=8044
# SSL Configuration - Disabled for local development
server.ssl.enabled=false
# server.ssl.key-store=file:/root/keystore.p12
# server.ssl.key-store-password=tomcat
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=myapp

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

app.quickation.items=Special Offers,DTH offers,Digital apps,Contact us,Service plan and packages,Bestselling mobile devices,Smart Solutions,Live Sports


elasticsearch.index.name=complete-conversation-
# elasticsearch.host=13.232.25.93
elasticsearch.port=9200
elasticsearch.host=localhost
# elasticsearch.port=9200
spring.elasticsearch.uris=http://localhost:9200
spring.elasticsearch.enabled=true
spring.elasticsearch.rest.connection-timeout=1000
spring.elasticsearch.rest.read-timeout=1000
spring.elasticsearch.connection-timeout=5000
spring.elasticsearch.socket-timeout=60000
spring.elasticsearch.connection-request-timeout=0

# Socket Configuration
# socket.host=localhost
# socket.port=8081
# socket.ping-timeout=60000
# socket.ping-interval=25000
# socket.agent-ping-interval=30000

# Socket Configuration
socket.host=0.0.0.0
socket.port=8099
socket.ping-timeout=60000
socket.ping-interval=25000
socket.agent-ping-interval=30000
# User ping timeout in milliseconds (default: 10 seconds)
socket.user.ping.timeout=10000

# Socket SSL Configuration - Disabled for local development
socket.ssl.enabled=false

# Redis Configuration - Local Environment
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=2000ms
spring.data.redis.database=0

# Redis Connection Pool - Local
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-wait=-1ms
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
